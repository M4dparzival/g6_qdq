# TODO: Update Binary Search Tree HTML Page

## Completed Tasks
- [x] Add three new options to the operation dropdown: "Get Minimum", "Get Maximum", and "Find Height" with values "get_min", "get_max", "find_height".
- [x] Update the toggleInputFields JavaScript function to hide the value input field for these new operations (since they don't require user input).
- [x] Add display blocks in the HTML to show the results for min_result, max_result, and height_result, styled similarly to traversal_result.
- [x] Keep the existing inputs, buttons, and tree visualization intact.

## Followup Steps
- [ ] Test the updated page to ensure the new operations work and display results correctly.
- [ ] No installations or additional dependencies needed.
